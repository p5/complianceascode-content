# platform = multi_platform_all
describe file('/proc/cmdline') do
{{%- if ARG_NEGATE %}}
  its('content') { should_not match(/{{{ ARG_NAME }}}={{{ ARG_VALUE }}}/) }
{{%- else %}}
  its('content') { should match(/{{{ ARG_NAME }}}={{{ ARG_VALUE }}}/) }
{{%- endif %}}
end

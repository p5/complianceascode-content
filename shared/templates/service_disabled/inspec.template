# platform = multi_platform_all
{{%- if init_system == "systemd" %}}
describe service('{{{ DAEMONNAME }}}') do
  it { should_not be_enabled }
  it { should_not be_running }
end

describe service('{{{ DAEMONNAME }}}.socket') do
  it { should_not be_enabled }
end
{{%- endif %}}
